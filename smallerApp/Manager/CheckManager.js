import { Piece } from "../Piece.js";
import { Square } from "../Square.js";
import { GameStateManager } from "../GameStateManage.js";
export class CheckManager {

    /**
     * Calculate wheter the following occur: 
     * 1. the move wwould reveal a check your king -> Illegal-move cannot check
     *  
     * @param {Square} toSquare 
     */
    static preMoveCheck(fromSquare, toSquare) {

    }
}